package com.droidexplorer.websim.debug

/**
 * Release-safe StrictMode stub. The real implementation lives in the debug source set.
 */
object StrictModeConfig {
    fun install() {
        // no-op in main to keep release builds lightweight
    }
}
